build/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock
.ccache/
*.log
!captures/*.log

# host test build. Both build directories hold the decoders generated from
# field-table.json (build/gen, build-host/gen): generated, never committed, so
# they cannot be edited into and cannot drift from the table.
build-host/
__pycache__/
